let age = prompt("How old are you");
let message = "";
message = age >= 18 ? "Allowed" : "Not allowed";
alert(message);

// let message;
// age >= 18 ? (message = "Hello") : (message = "Not allowed");
// alert(message);
